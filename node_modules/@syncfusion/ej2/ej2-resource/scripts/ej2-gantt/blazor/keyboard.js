import * as cls from './css-constants';
var KeyboardHandler = (function () {
    function KeyboardHandler(parent) {
        this.parent = parent;
    }
    KeyboardHandler.prototype.taskbarFocus = function (tdElem) {
        var taskbarElem = tdElem.querySelector('.' + cls.traceChildTaskBar);
        var parentTaskbarElem = tdElem.querySelector('.' + cls.traceParentTaskBar);
        if (taskbarElem) {
            taskbarElem.focus();
            tdElem.querySelector('.' + cls.taskBarMainContainer).querySelector('.' + cls.traceChildTaskBar).classList.add(cls.Active);
        }
        else {
            parentTaskbarElem.focus();
            tdElem.querySelector('.' + cls.taskBarMainContainer).querySelector('.' + cls.traceParentTaskBar).classList.add(cls.Active);
        }
        return "isNextTaskbar";
    };
    KeyboardHandler.prototype.RightLabelFocus = function (tdElem) {
        var rightLabelElem = tdElem.querySelector('.' + cls.rightLabelContainer);
        rightLabelElem.focus();
        rightLabelElem.querySelector('.e-label').classList.add(cls.Active);
        return "isNextRightLabel";
    };
    KeyboardHandler.prototype.LeftLabelFocus = function (tdElem) {
        var leftLabelElem = tdElem.querySelector('.' + cls.leftLabelContainer);
        leftLabelElem.focus();
        leftLabelElem.querySelector('.e-label').classList.add(cls.Active);
        return "isNextLeftLabel";
    };
    return KeyboardHandler;
}());
export { KeyboardHandler };
